<nav class="navbar navbar-expand-lg bg-dark border-bottom border-body" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand text-white" href="/superadmin">MyBlog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse">
      
      <% if(!locals.user) { %>
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active text-white" aria-current="page" href="/superadmin">SuperAdminHome</a>
        </li>
        <% if (locals.superadmin) { %>
          <div class="container-fluid">
            <div class="collapse navbar-collapse">
              <ul class="navbar-nav">
                <li class="nav-item dropdown">
                  <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    Admins
                  </button>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/superadmin/allApprovedAdmin">Approved Admin</a></li>
                    <li><a class="dropdown-item" href="/superadmin/allNotApprovedAdmin">NotApproved Admin</a></li>
                  </ul>
                </li>
              </ul>
            </div>
          </div>
          <div class="container-fluid">
            <div class="collapse navbar-collapse">
              <ul class="navbar-nav">
                <li class="nav-item dropdown">
                  <button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    Users
                  </button>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/superadmin/allApprovedUser">Approved User</a></li>
                    <li><a class="dropdown-item" href="/superadmin/allNotApprovedUser">NotApproved User</a></li>
                  </ul>
                </li>
              </ul>
            </div>
          </div>
          <li class="nav-item">
            <a class="nav-link text-white" href="/superadmin/allblog">Blogs</a>
          </li>
          <% } %>
          </ul>
          <% } %>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
  <div class="container mt-2 w-50 rounded">
    <div class="alert alert-danger alert-dismissible fade show" id="Alert" role="alert">
      <%= locals.error %>
    </div>
  </div>
  <% } %>
  
<% if (locals.success) { %>
    <div class="container mt-2 w-50 rounded">
      <div class="alert alert-success alert-dismissible fade show" id="Alert" role="alert">
        <%= locals.success %>
      </div>
  </div>
<% } %>

<% if (locals.msg) { %>
    <div class="container mt-2 w-50 rounded">
      <div class="alert alert-danger alert-dismissible fade show"  id="Alert" role="alert">
        <%= locals.msg %>
      </div>
    </div>
<% } %>

<% if (locals.waitingMsg) { %>
    <div class="container mt-2 w-50 rounded">
      <div class="alert alert-warning alert-dismissible fade show" id="Alert" role="alert">
        <%= locals.waitingMsg %> 
      </div>
    </div>
<% } %>

<script>
  document.addEventListener('DOMContentLoaded', () =>{
    setTimeout(() => {
      const alertBox = document.getElementById('Alert');
      if (alertBox) {
        alertBox.classList.remove('show');
        alertBox.classList.add('fade');
        setTimeout(() => alertBox.remove(), 500); 
      }
    }, 1800);
  })
</script>